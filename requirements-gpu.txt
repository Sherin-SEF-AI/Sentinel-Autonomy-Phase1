# ============================================================================
# SENTINEL Safety Intelligence Platform - GPU/CUDA Dependencies
# ============================================================================
# 
# This file contains GPU-accelerated variants of packages for NVIDIA CUDA.
# Requires: NVIDIA GPU with compute capability >= 7.0, CUDA 11.8+
#
# Installation:
#   pip install -r requirements-gpu.txt
# ============================================================================

# -----------------------------------------------------------------------------
# Core Python Utilities
# -----------------------------------------------------------------------------
numpy>=1.24.0              # Vectorized numerical operations for performance
PyYAML>=6.0                # YAML configuration file parsing
tqdm>=4.66.0               # Progress bars for model loading and processing

# -----------------------------------------------------------------------------
# Computer Vision & Image Processing
# -----------------------------------------------------------------------------
mediapipe>=0.10.0          # Face detection, facial landmarks extraction for DMS
opencv-python>=4.8.0       # Camera capture, calibration, BEV transformation
pillow>=10.0.0             # Image I/O and manipulation

# -----------------------------------------------------------------------------
# Machine Learning & Deep Learning (GPU-Accelerated)
# -----------------------------------------------------------------------------
filterpy>=1.4.5            # Kalman filtering for object tracking and velocity estimation
onnx>=1.14.0               # Model portability and deployment format
onnxruntime-gpu>=1.15.0    # GPU-accelerated ONNX model inference (CUDA)
scikit-learn>=1.3.0        # Hungarian algorithm for multi-view detection fusion
scipy>=1.11.0              # Scientific computing for trajectory prediction

# PyTorch with CUDA 11.8 support
torch>=2.0.0               # Deep learning framework with CUDA acceleration
torchvision>=0.15.0        # Vision models and transformations with CUDA support
# Note: For specific CUDA version, use:
# torch>=2.0.0+cu118 --index-url https://download.pytorch.org/whl/cu118
# torchvision>=0.15.0+cu118 --index-url https://download.pytorch.org/whl/cu118

ultralytics>=8.0.0         # YOLOv8 for multi-view object detection (GPU-accelerated)

# -----------------------------------------------------------------------------
# Optional: TensorRT for Production Optimization
# -----------------------------------------------------------------------------
# tensorrt>=8.6.0          # NVIDIA TensorRT for optimized inference (15ms target)
# Note: TensorRT requires separate installation from NVIDIA
# See: https://docs.nvidia.com/deeplearning/tensorrt/install-guide/

# -----------------------------------------------------------------------------
# Web Backend & API
# -----------------------------------------------------------------------------
fastapi>=0.103.0           # REST API and WebSocket server for visualization dashboard
pydantic>=2.0.0            # Data validation and serialization
python-multipart>=0.0.6    # Multipart form data parsing for file uploads
uvicorn[standard]>=0.23.0  # ASGI server for FastAPI backend
websockets>=11.0           # Real-time data streaming to frontend

# -----------------------------------------------------------------------------
# Development & Testing
# -----------------------------------------------------------------------------
pytest>=7.4.0              # Unit and integration testing framework
pytest-asyncio>=0.21.0     # Async test support for FastAPI endpoints
pytest-cov>=4.1.0          # Code coverage reporting
